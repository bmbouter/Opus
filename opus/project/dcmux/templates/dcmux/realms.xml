<realms>
    {% for realm in realms %}
        <realm href="{{ realms_uri }}/{{ realm.id }}">
            <id>{{ realm.id }}</id>
            <name>{{ realm.name }}</name>
            <state>{{ realm.state }}</state>
            <limit/>
        </realm>
    {% endfor %}
</realms>
