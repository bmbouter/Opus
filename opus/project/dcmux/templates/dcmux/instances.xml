<instances>
    {% for instance in instances %}
        <instance href="{{ instances_uri }}/{{ instance.id }}">
            <id>{{ instance.id }}</id>
            <owner_id>{{ instance.owner_id }}</owner_id>
            <name>{{ instance.name }}</name>
            <hardware_profile href="{{ instance.image.hardware_profile }}"/>
            <realm href="{{ instance.realm }}" />
        </instance>
    {% endfor %}
</instances>
